import { expect } from 'chai';
import RawHtmlAttributeUpdater from '../target/raw-html-attribute-updater';

describe('the raw-html-attribute-updater module', () => {
	it('should instantiate a raw-html-attribute-updater', () => {

	});
});